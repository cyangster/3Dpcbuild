/** Strong visual when a part is selected (e.g. from sidebar) — cyan “glow” read in 3D. */
export const SELECTION = {
  surface: "#38bdf8",
  emissive: "#0369a1",
  emissiveIntensity: 0.82,
  metalnessSelected: 0.28,
  roughnessSelected: 0.42,
};

export const HOVER = {
  surface: "#60a5fa",
  emissive: "#1d4ed8",
  emissiveIntensity: 0.28,
};
