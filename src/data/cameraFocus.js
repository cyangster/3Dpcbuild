/**
 * Default orbit pose in world space (matches PC root group at y = -0.15).
 * Target nudged toward the motherboard / M.2 side so the SSD reads from the opening.
 */
export const DEFAULT_CAMERA = {
  target: [0.08, 0.46, -0.06],
  position: [1.32, 0.69, 1.48],
};
